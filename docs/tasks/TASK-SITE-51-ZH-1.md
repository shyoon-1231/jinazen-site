# TASK-SITE-51-ZH-1 — 홈페이지 중국어(간체) 판

- 착수 **2026-09-25 07:44 KST** · `main` · 착수 HEAD `1264900` · behind 0 · 추적 변경 0
- 사본(되돌리기용) — `…/scratchpad/site51-bak/` (`*.bak` 착수 시점)
- 커밋 둘 — `50d0e4c`(§2 `/zh/` · hreflang · sitemap) · 이 기록 · 🔴 **§3 은 하지 않았다**(아래)
- 🔴 `privacy/` 한국어판 · 사업자정보 여덟 · DNS · Pages 설정 · Worker · Route 변경 **0**
- 🔴 `style.css` 변경 **0** (토큰도 규칙도) · 🔴 `jinazen` · `jinazen-web` 은 **읽기만** 했다

## 🔴 §1 ① 결과지 중국어는 **간체**다 — 그래서 홈페이지도 간체로 간다

| 어디 | 무엇으로 확인했나 | 간체 | 번체 |
|---|---|---|---|
| 매장 키오스크 UI | `android_app/mindmap_kiosk/app/src/main/res/` — 있는 중국어 자원이 **`values-zh-rCN` 하나**다 | 业个关动势发号对开录择时画确纸计认记请财运这选闭间页 … | **0자** |
| 🔴 **결과지 문안** | `engine/reporting/report_copy_i18n.py` · 열쇠 **`zh-CN`** (137칸) · `SUPPORTED_LOCALES = ("ko","en","ja","zh-CN")` | 为价关动势发对将岁开录报时机样现确结计请运这闭间马鸡龙 | **0자** |

- ⚠ **파일을 통째로 훑으면 번체가 나온다** — 그것은 **일본어 칸**(動·勢·時·現·確·結·計·運·開·間·報)이다.
  🔴 `zh-CN` 칸만 뽑아 다시 세어 **0자**를 받았다. 갈래를 안 가르고 셌으면 「번체가 섞였다」로 잘못 멈출 뻔했다.
- ⚠ `values-zh-rTW` · `values-zh-rHK` 는 **빌드 중간 산출물에만** 있다(딸려 온 라이브러리 것) — 소스에는 없다.
- 🟢 **그래서 §2 의 「门店自助机可为您提供中文结果单。」 한 줄을 넣었다.** 간체가 아니었으면 이 줄을 안 넣고 멈췄다.

## 🔴 §1 ② 「中文」을 더하면 **en 쪽이 761~777px 에서 두 줄이 된다** — §3 을 하지 않았다

닻 **높이**로 쟀다(`.menu` 는 `flex-wrap` 이 없어 y 로는 안 잡힌다 · SITE-46/47/48 방식).
여유는 `space-between` 이 남기는 **한쪽 틈**이다.

| 761px 여유 | ko | en | ja | zh |
|---|---|---|---|---|
| 지금(3언어) | 64.3 | 🔴 **8.3** | 27.4 | — |
| 「中文」 더하면 | 48.0 | 🔴 **0.0 · 두 줄** | 11.0 | — |
| 🟢 `/zh/` 쪽 자신(4언어) | — | — | — | **44.7** |

- 🔴 **깨지는 구간은 761~777px** 이다(1px 씩 넓혀 첫 한 줄 폭이 **778px · 여유 0.5** 임을 실측).
- ⚠ 「中文」은 언어 칸을 **71.4 → 104.1px** 로 넓힌다(+32.7 · 한쪽 틈으로는 +16.3). en 에는 **8.3 뿐**이었다.

### 🔴 선택지 — **고르지 않았다.** 소유자가 정한다

| 선택지 | 761px en 여유 | 비고 |
|---|---|---|
| A 언어 칸 틈 `10px → 6px` | 🔴 **0.0 · 두 줄** | 모자란다 |
| B 라벨 「ZH」 | 🔴 **0.0 · 두 줄** | 모자란다. ⚠ 중국 손님이 「中文」보다 못 알아본다 |
| C 메뉴 틈 `16px → 12px`(≤900) | 🟢 **4.0 · 한 줄** | 🔴 지금(8.3)보다 **더 빠듯해진다** |
| A+C 함께 | 🟢 **10.0 · 한 줄** | 지금보다 조금 넉넉하다 |
| D 언어를 **펼침 목록 하나**로 | 못 쟀다 | 칸 하나로 줄지만 **새 부품**이다(스크립트·초점·키보드) |
| E 메뉴 숨김 경계를 `760 → 790px` 로 | 못 쟀다 | 깨지는 구간을 **폰 쪽에 넘긴다**. 그 구간에서 메뉴 여섯이 사라진다 |

⚠ **C 를 혼자 쓰면 여유가 4.0px 이다.** SITE-47 이 8.3px 을 「다음에 라벨을 한 글자라도 늘리면
761px 에서 두 줄이 된다」고 적어 둔 값인데, 그 절반이 된다.

## §2 `/zh/index.html` (`50d0e4c`)

- 🔴 **간체 · `lang="zh-Hans"` · `og:locale` `zh_CN` · JSON-LD `inLanguage` `zh-Hans`**
- 🔴 **hreflang 넷(ko·en·ja·zh-Hans)+x-default 를 네 쪽 모두에** · `sitemap.xml` 에 `/zh/`
  ⚠ 기존 세 쪽은 **그 한 줄 말고 한 글자도** 안 바뀌었다
- 🔴 **브랜드는 `JINAZEN`** — 중국어 브랜드명을 짓지 않았다(소유자 확정). 사람 이름·상호도 한자로
  지어내지 않았다: `Jina Kim` · `Yoondam` · `feelingTV` · `East Friends`. 「김진아」는 한글로 병기한다
- 🟢 **철학 띠는 초안이 아니다** — 결과지 정본(`_PROLOGUE` · `zh-CN` · 확정 2026-09-09)의 **글자 그대로**다.
  손님이 **종이에서 읽는 글자와 같다.** ⚠ 제목만 줄바꿈 자리가 다르다(정본은 한 줄 · 이 띠는 쉼표에서 `<br>`)
- 🔴 **더한 두 줄**(소유자 확정):
  · 배너 ② 「本服务为韩语服务 · 需要 Kakao 账号」 — 원문 「한국어 서비스입니다 · 카카오 계정이 필요합니다」
  · 공간 「门店自助机可为您提供中文结果单。」 — 원문 「매장 키오스크는 중국어로 결과지를 드립니다」
- 🔴 **`/zh/privacy/` 를 만들지 않았다** — 법무는 한국어만. 푸터 링크는 한국어 방침으로 보내고
  글자에 **「（韩语）」**를 붙여 **누르기 전에** 알린다
- 🔴 **Play 단추 그대로** — 다른 스토어를 지어내지 않았다
- 🔴 **중국어 UI 를 약속하지 않았다** — `fortunegraph` 에는 `values`(한국어) 하나뿐이고 중국어 계획을
  확인하지 못했다. ⚠ en·ja 쪽에는 「준비 중」이 적혀 있는데 이 쪽에는 **지금 사실만** 적었다(D-023)
- ⚠ **차 재료 한 마디를 넣지 않았다** — en·ja 에는 있으나 **한국어 정문에는 없다.** 정문을 따랐다
- ⚠ `?mode=privacy` 스크립트는 **한국어 쪽에만** 있다(Play 등록 주소) — 이 쪽에 두지 않았다

### 🔴 뼈대 대조 — 구조가 밀리지 않았다

글자·주석을 걷고 **태그 · class/id/role/rel/lang · 주소**만 남겨 `en` 과 견줬다(819 대 827).
차이 **스무 줄이 전부 의도한 것**이다: `lang` · `canonical` · hreflang 한 줄 · `og:locale` ·
언어 전환(EN 이 링크로 · 中文 이 현재) · 더한 두 줄 · `small lang="ko"` · 방침 링크.

## 🔴 §3 언어 전환 — **하지 않았다**

지시서가 「§1② 에서 넘쳤으면 이 절을 하지 않고 멈춘다」고 정해 두었고, 넘쳤다.

⚠ **그래서 ko·en·ja 에서 `/zh/` 로 누를 자리가 없다.** 지금 닿는 길은 **`hreflang` 과
`sitemap.xml`**(검색엔진) 과 주소를 직접 치는 것뿐이다.
⚠ **`/zh/` 쪽 자신의 언어 전환에는 넷이 있다**(KO·EN·JA·**中文**) — 그러지 않으면 들어온 손님이
다른 언어로 **나갈 수 없다**. 🔴 그 쪽에서는 761px 여유가 **44.7px** 라 넘치지 않는다(실측).

## §4 로컬 확인 — 🟢 **128 항목 통과 · 실패 0**

네 쪽 200 · h1 쪽마다 1 · alt 누락 0 · JSON-LD 파싱 · **hreflang 넷+x-default** ·
메뉴 761·800·900·1280 한 줄(닻 높이) · 폰 400 가로 넘침 0 · 첫 화면 점 셋 ·
`?mode=privacy` → 방침 · 반증 셋(ko 만) · 🟢 web_report 대조 시험 **115 통과**.

| 판매 단추 (네 언어 동일) | 시작하기 | 예시 | Play | mailto | tel |
|---|---|---|---|---|---|
| | **3** | **2** | **2** | **3** | **2** |

폰 머리띠 — 네 쪽 × 360·400·600·760px 에서 **보이는 메뉴 1칸 · 숨은 6칸 · 가로 넘침 0**.
`/zh/` 는 언어 칸이 **넷**(KO·EN·JA·中文)이고 넘치지 않는다.

### 🔴 검사기를 변이 일곱으로 먼저 시험했다 — 전부 빨개졌다

en 에서 zh hreflang 뺌 · zh 의 ko hreflang 주소 틀리게 · h1 둘 · alt 지움 ·
JSON-LD 깨뜨림 · 판매 단추 하나 뺌 · 카드 넷→셋.

## §5 배포

### 배포 전 지문 (비컨 걷고 **3회씩** · 아홉 주소 전부 3/3 일치)

| 주소 | 지문 |
|---|---|
| `/` · `/en/` · `/ja/` | `610cff02` · `ba3e5684` · `ae0afc0b` |
| 🔴 `/zh/` | `610cff02` — ⚠ **없는 쪽인데 200 이고 첫 화면과 같다.** 이 사이트는 모르는 주소에 **홈을 낸다**(`/auth/me` 도 그랬다). 배포 뒤 이 값이 바뀌어야 한다 |
| 🔴 `/privacy/` 셋 | `00bab904` · `1421d16f` · `b8e67629` |
| 🔴 `robots.txt` · `sitemap.xml` | `84da6283` · `92f49a62` (🔴 sitemap 은 이번에 **바뀐다**) |

⚠ **배포 뒤 확인 결과는 이 문서에 없다** — 지시서가 **push 를 맨 끝에 한 번**으로 정해 두어 이
기록이 그 push 에 **함께 실려 나가기 때문이다.** 라이브 확인은 push 뒤에 했고 결과는 TASK 보고에 있다.
🔴 어긋난 것이 있었으면 **되돌림 커밋**이 이 다음에 있다 — 없으면 어긋난 것이 없었다는 뜻이다.

## 미검증

- 🔴 **번역 전체가 원어민 검토 전이다** — 철학 띠(정본)만 예외다
- 🔴 **§3 을 소유자가 정해야 이 쪽에 손님이 닿는다** — 지금은 누를 자리가 없다
- ⚠ 「김진아」의 **한자 표기**를 확인하지 못해 `Jina Kim` 으로 두었다
- ⚠ 중국어 **UI 계획**(Play 앱)을 확인하지 못해 약속하지 않았다
- ⚠ 「솟대」를 「神竿（솟대）」로 옮겼다 — 정해진 중국어 표기를 확인하지 못했다
