# TASK-SITE-50-HEADER-LOGIN-1 — 머리띠 로그인/마이페이지 · 공간 체험 카드 위로 · 회사소개 빼기

- 착수 **2026-09-25 14:06 KST** · 종료 **14:35 KST** (이 기록 커밋 직전) · `main` · 착수 HEAD `6472dae` · behind 0 · 추적 변경 0
- 사본(되돌리기용) — `…/scratchpad/site50-bak/` · 🔴 **git 으로 되돌린 적 0 · revert 0**
- 커밋 다섯 — §1 `3107ffa` · §2 `b142dcf` · §3 `c9a268d` · §4 `6ad20de` · 이 기록
- 🔴 `privacy/` · 사업자정보 여덟 항목 · DNS · Pages · Worker · Route 변경 **0** · `:root` 토큰 변경 **0**
- 🔴 네이버 소유확인 메타 **무변경** — 검사기가 네 쪽에서 값을 센다

## 착수 전 — 라이브 `/report/auth/me` (비로그인 · `Accept: application/json`)

`200` · `{"loggedIn":false}` · 열쇠 **하나** · `cache-control: no-store`.
로그인 시 모양은 `jinazen-web/web_report/tests/test_auth_me_70.py` 가 `== {"loggedIn": True}` 로 잠근다(읽기만 함).

## §1 머리띠 — 로그인 전 「로그인」 · 뒤 「마이페이지」

- 기본(HTML) = 「로그인」 · `https://jinazen.com/login/?next=/mypage/` — 🔴 스크립트가 꺼져도 이대로 맞다
- 새 파일 `assets/account.js` — 답이 **200 · JSON · 열쇠 `loggedIn` 하나 · 값 `true`(참/거짓 값)** 일 때만
  「마이페이지」 · `https://jinazen.com/mypage/` 로 바꾼다. 실패 · 2초 초과 · 모양이 다르면 그대로
- 🔴 칸 폭 고정 — 두 라벨을 같은 grid 칸에 겹쳐 두고 `visibility` 로 하나만 보인다(`style.css` 새 규칙 넷 · 새 값 0).
  `visibility:hidden` 글자는 접근성 이름에도 안 실린다(검사기로 확인)
- 라벨: ko 로그인/마이페이지 · en **Login/My Page** · ja ログイン/マイページ · zh 登录/我的页面 (en·ja·zh 초안 · 원문 주석)
  ⚠ en 은 종전 「My page」 → 「My Page」(지시서 글자 그대로)

### 메뉴 한쪽 여유 (닻 높이 · 네 언어 전부 한 줄)

| 폭 | ko | en | ja | zh |
|---|---|---|---|---|
| **761** | 66.0 | **9.8** (전 10.0) | 29.0 | 62.7 |
| 777 | 74.0 | 17.8 | 37.0 | 70.7 |
| 900 | 135.5 | 79.3 | 98.5 | 132.2 |
| 901 | 94.0 | 37.8 | 57.0 | 90.7 |
| 1280 | 203.5 | 147.3 | 166.5 | 200.2 |

🟢 en 761 **9.8 ≥ 8.3**(멈춤선). 줄어든 0.2px 는 대문자 P 몫이다. 로그인/비로그인 두 상태의 칸 폭 · 자리 · 여유 차이 **0**.

## §2 공간 — 체험 카드 넷을 제목 바로 아래로

차례: 제목 · 부제 → **카드 넷** → 글 문단 → 그림 두 장 → 주소 · 개시일 (zh 는 글 문단 다섯 — 종전 그대로)
- 제목 · 부제 · 카드를 종전 카드 띠(밝은 `.teas .path .space-cards`)로 올리고 `id="space"` 를 옮겼다 — 새 CSS 0
- 글 · 그림은 짙은 띠 그대로 · 주소 · 개시일은 그림 **아래**(오른쪽 열) — 폰에서도 「글 → 그림 → 주소」
- 🔴 옮기기 전후 **글자 대조**(태그·주석 걷고 정렬) 통과 — 카드 글자 · 순서 · 「이스트프렌즈에서 보기 ↗」 무변경
- ⚠ 제목 모양이 바뀐다 — 짙은 띠의 금색 강조 → 밝은 띠의 굵은 검정 · {{소유자 확인}}

## §3 푸터 「회사소개」 빼기

`#company` 로 가는 링크 — 사이트 전체에서 **푸터 넷(언어별 하나)뿐**이었다. 다른 곳 **0** · `jinazen-web` 에서도 0.
닻 `company` 는 그대로 둔다 · 방침 링크 · `?mode=privacy` 무변경.

## §4 매장 도입 그림 빼기

`#store` 의 site_06 한 장과 오른쪽 열을 걷었다. 공간 띠의 두 장은 그대로 · 그림 파일 삭제 0.
쪽 안 되풀이 그림: site_06 **2 → 0** (northstar 2 는 로고 + 배너 ① 표식 · 종전 그대로).
⚠ 매장 도입 띠 PC 오른쪽이 비어 보인다 · {{소유자 확인}}

## §5 확인

🔴 **검사기를 깨뜨린 표본으로 먼저 시험했다** — 변이 열둘 가운데 아래 열하나가 첫 번에 빨개졌다:
auth/me 무시하고 늘 바꿈 · 절대 안 바꿈 · 열쇠 수 검사 뺌 · 시간 초과 20초 · 폭 고정 깸(둘) · 기본 링크를 /mypage/ 로 ·
카드 ①② 자리 바꿈 · 카드를 글 아래로(§2 전 모양) · ja 회사소개 되살림 · zh 매장 도입 그림 되살림.
⚠ **하나는 처음에 초록이었다** — 「늦은 답 버리기(late)」 검사만 뺀 변이. 2초에 `abort()` 가 요청을 끊어
늦은 `true` 가 애초에 오지 않았다 — **변이가 동작을 못 깼다**(함정 30 과 같은 모양). 시간 초과 자체를 없앤 변이로 다시 재 빨개졌다.

- 머리띠 검사기(`check_account.py`) **88 통과 · 실패 0** — jinazen.com 주소를 그대로 열고 요청을 가로채
  **같은 출처**로 쟀다(라이브 서버에 닿지 않음). 시나리오 15 × 네 언어 · 스크립트 꺼짐 · 접근성 이름 · 폰 400
- 사이트 검사기(`check_site.py`) **266 통과 · 실패 0** — 200 · h1 하나 · alt 누락 0 · JSON-LD · hreflang ·
  `?mode=privacy` 와 반증 셋 · 판매 단추(전·후 같음: 시작하기 2 · 예시 2 · Play 2 · mailto 2 · tel 1) · 네이버 값 · 폰 400 넘침 0
- 주석 여닫는 수 — ko **47/47** · en **64/64** · ja **64/64** · zh **65/65**
- `web_report` 대조 시험(`test_app_cta` · `test_legal_pages` · `test_app_parity` · `test_seo_71` · `test_design_tokens`) **126 통과 · 실패 0**
  ⚠ 지시서의 「115」와 수가 다르다 — 이 다섯 파일의 현재 수다(WEB-71 이후 늘었다). 제목 계약은 풀려 있다.

## 배포 전 지문 (비컨 걷고 3회 · 아홉 주소 3/3)

`/` `a93580bc` · `/en/` `f8323359` · `/ja/` `b01a4fcd` · `/zh/` `dd6ada45` ·
🔴 `privacy/` 셋 `00bab904`·`1421d16f`·`b8e67629` · `robots` `84da6283` · `sitemap` `6d98deeb` (SITE-53 기록과 같다 · 그대로여야 한다)

⚠ 배포 뒤 확인은 TASK 보고에 있다(이 기록이 그 push 에 함께 실린다).

## 미검증

- 🔴 **로그인한 브라우저에서 「마이페이지」** — 소유자 확인(로그인 쿠키로는 재지 않았다)
- ⚠ 공간 제목 모양 변화 · 매장 도입 띠 오른쪽 빈자리 — 소유자 확인
- ⚠ en·ja·zh 라벨은 번역 초안
