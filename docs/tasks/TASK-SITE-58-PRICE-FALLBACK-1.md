# TASK-SITE-58-PRICE-FALLBACK-1 — 창구 실패 시 문장 숨김 · 외국어 쪽 딱지 숨김

- 착수 **2026-09-26 00:41 KST** · 종료 **08:20 KST** (이 기록 커밋 직전) · `main` · 착수 HEAD `18825c6`(SITE-57 끝 · 라이브 = 로컬) · behind 0 · 추적 변경 0
- 사본(되돌리기용) — `/private/tmp/claude-502/-Users-sh-yoon-dev-jinazen/ecd28bd2-de62-4b52-9b20-f914cc426b5c/scratchpad/site58-bak/` · 변이 사본 `…/mut58/N1~N3.bak` · 🔴 **git 으로 되돌린 적 0**
- 커밋 둘 — `15cd9d2` · 이 기록
- 🔴 `privacy/` · 사업자정보 · DNS · Pages · Worker · Route · `:root` 토큰 · 네이버 메타 · 머리띠 로그인(`account.js` · 머리 인라인) **무변경**(사본과 대조) · style.css 지운 줄 **0** · `price.js` 는 **주석 4줄만**

## 무엇을 했나

- **① 가격 문장 통째로 숨김** — 개인용 섹션 `p.price-say` 에 `data-price-when` 을 붙이고 CSS 가 **기본 `display:none`**.
  `price.js` 가 가격을 채울 때 붙이는 `.is-priced` 가 있어야 보인다 → 🔴 503 · 2초 초과 · 모양 다름 · **스크립트 꺼짐**에도 숨는다.
  ~~SITE-57 — 못 읽으면 문장 안 가격 자리만 빈 칸~~
- **② 딱지는 ko 쪽에만** — en·ja·zh 의 딱지 칸 둘(배너 ② · 개인용 섹션)을 **HTML 에서 뺐다**. 딱지 글자가 관리 화면의 한국어뿐이다.
  `price.js` 는 칸이 있는 곳에만 채우므로 코드는 그대로다.

## 🔴 판정 — 문장이 나타날 때 아래가 밀려도 되는가: **된다(자리를 잡아 두지 않는다)**

| 근거 | 값 |
|---|---|
| 자리를 잡아 두면 | 못 읽을 때 **빈 줄**이 남는다 — 「빈 칸을 보이지 않는다」 확정과 어긋난다 |
| 밀리는 크기(문장을 보고 있을 때) | **+42px(1280) · +69px(400)** — 스크롤 고정을 끈 변이 N3 로 잰 값 |
| 손님이 이미 카드를 보고 있을 때(가격이 1.5초 늦게 옴) | 보던 카드 위치 변화 **≤ 0.7px** — 네 언어 × 400 · 1280, 브라우저 스크롤 고정(Chromium) |
| 섹션 자리 | 첫 화면 밖(철학 · 공간 · 네 걸음 다음) — 창구는 보통 그 전에 답한다 |

⚠ 스크롤 고정은 Chromium 에서 잰 값이다 — 다른 브라우저(특히 iOS Safari)는 {{미검증}}.

## 확인

- 🔴 **검사기를 먼저 바꾼 뒤 바꾸기 전 판에 대고 돌렸다** — **116 빨강**(문장 숨김 · 외국어 딱지 12 · 늦게 나타남)
- 🔴 **변이** — 셋 다 빨강(파일이 바뀐 것을 `cmp` 로 먼저 확인 · 되돌린 뒤 `cmp` 같음):
  N1 숨김 규칙 빼기(실패에 빈 칸 보이기) → **104** · N2 en 배너에 딱지 칸 되살리기 → **4** · N3 스크롤 고정 끄기 → **8**(+42.3 · +68.7px)
- 가격 검사기 **1035 통과 · 실패 0** — 네 언어 × 400 · 1280 × 여덟 상태 + 늦게 나타남 8
- 사이트 검사기 **310** · 머리띠 **88** — 실패 0 · 메뉴 폭 **SITE-57 전과 같음**(en 761 9.8) · 판매 단추 그대로(`/report/new` 2)
- ⚠ 깜박임 검사기(머리띠) — 일곱 번 중 **다섯 번 80/0**. 둘째 회 1건은 알려진 「기억 없음 + 300ms」 시간 경계(SITE-55 기록과 같은 항목),
  🔴 **첫 회 4건은 항목을 남기지 못했다.** 머리띠 파일은 사본과 같다.
- 주석 여닫는 수 ko **56/56** · en **74/74** · ja **74/74** · zh **75/75**

## 배포 전 지문

라이브(배포 전) `/` `73f0d5ac` · `/en/` `6af4100e` · `/ja/` `05a5d9dd` · `/zh/` `eb09fb19` · privacy 셋 `00bab904` · `1421d16f` · `b8e67629`
기대(로컬) `/` `201d07dc` · `/en/` `f5453b1f` · `/ja/` `fc451697` · `/zh/` `0c909e00`
