# TASK-SITE-40-STORY-1 — 홈페이지 재구성: 철학 → 흐름 → 온라인·공간 → 제품

- 착수 2026-09-22 19:58 KST · 종료는 보고에 실제 시각으로 · 🔴 브랜치 `story-1` (착수 main `0842a7c` · behind 0 · 추적 파일 변경 0)
- 🔴 **main 에 합치지 않았다** — 소유자가 갤러리(`docs/screenshots/story-1/index.html`)를 본 뒤다
- 🔴 `privacy/` · 푸터 사업자정보 여덟 항목 · DNS · Pages 설정 · Worker · Route · `_redirects` — **변경 0**
- 🔴 `style.css` 토큰 변경 0 — **지운 줄 0 · 더한 규칙 여섯**(아래 목록)
- 🔴 새 그림 0장 · 가격 글자 0

## §0 바꾸기 전 구성 (main `0842a7c`)

| # | id | 제목 | 내용 | 판매 경로 |
|---|---|---|---|---|
| 1 | (없음) `.philo` | 「명상이 예술이 되는 순간」 · 「진아젠 JINAZEN」 | 사진 띠 · 단추 둘(시작하기 · 예시 화면 보기) | 🟢 일반인 |
| 2 | `#fortune` `.hero` | **h1** 「진아젠 운세그래프」 | eyebrow · lead · Play 단추 · 가격 안내 · 대운 그래프 그림 | 🟢 전문가 |
| 3 | `#report` | 「진아젠 운명리포트」 | 서비스 설명(약관 제4조) · 잔글씨 · 단추 둘 | 🟢 일반인 |
| 4 | (없음) `.usp` | 「소우주로서의 인간, 음양오행으로 풀어낸 삶의 지도」 | 🔴 **소유자 원문 세 문단**(2026-09-16) — 지시서 §2 의 철학 문안과 **글자까지 같다** | — |
| 5 | (없음) `.three` | 사주 명반 · 자미두수 · 타로운명수 | 앱 기능 셋 | 전문가 |
| 6 | (없음) `.feature` | 「한 사람의 운명을, 한곳에서 입체적으로」 | 고객 관리 · 그림 site_02 | 전문가 |
| 7 | (없음) `.feature.flip` | 「기계가 할 일과 사람이 할 일」 | 그림 site_03 | 전문가 |
| 8 | `#artist` | 김진아 | 명상아티스트 | — |
| 9 | `#teas` | 「해와 달과 별, 일월성신을 차로」 | 차 셋 | — |
| 10 | `#store` | 「매장 도입 문의」 | 전화 · 메일 | 🟢 매장 |
| 11 | `#company` · `#contact` | JINAZEN · 문의하기 | 회사소개 · 연락처 | — |

**판매 단추 (main)** — 시작하기 3(메뉴 · 첫 화면 · #report) · 예시 2(첫 화면 · #report) · Play 1(히어로) · 매장 문의 mailto 1(#store). 세 언어 같음.

**메뉴 여섯 (main)** — 운세그래프 `#fortune` · 운명리포트 `/report/` · 명상아티스트 `#artist` · 매장 도입 `#store` · 회사소개 `#company` · 문의하기 `#contact`.

## §1 새 순서 · h1 판정

첫 화면 → **#philosophy** → **#path** → #report → **#space** → **#app** → 기능 셋 · 한곳에서 · 기계와 사람 → #artist → #teas → #store → #company · 푸터.
⚠ 지시서 목록에 없던 기능 셋(5·6·7)은 **#app 바로 뒤**에 두었다 — 앱 설명이다. 차(#teas)는 명상아티스트 뒤 그대로.

**h1 = 첫 화면 「진아젠 JINAZEN」** (태그만 p → h1 · 글자·모양 그대로)
- 페이지가 「앱 소개」에서 「브랜드」로 바뀌었다 — 첫 제목이 브랜드여야 한다. 철학 제목은 문장이라 h1 으로 길고, 짙은 띠 규칙(`.usp h2`)을 h1 용으로 새로 적어야 한다
- 🔴 **앱 이름은 그대로 검색에 잡힌다** — `<title>` · `og:title` · JSON-LD `SoftwareApplication.name` 이 전부 「진아젠 운세그래프」 그대로다(안 고쳤다). #app 의 h2 에도 있다
- Play 스토어 연결은 h1 이 아니라 **Play Console 의 개발자 웹사이트 주소**(jinazen.com)와 `installUrl` 로 이어진다 — 둘 다 그대로
- ⚠ 첫 화면이 안 움직이게 `.philo h1.line2{line-height:1.7}` 를 더했다 — 실측: 사진 띠 안 **픽셀 차이 0줄**(400 · 1280)

## §2 문안 자리

| 자리 | 글자 | 출처 |
|---|---|---|
| #philosophy 제목 · 세 문단 | 소유자 문안 | 🔴 **이미 있던 `.usp` 띠 그대로**(한 글자도 안 바꿈) · id 만 달았다 |
| #path 카드 넷 제목 · 한 줄 · 단계 이름 | 소유자 문안 (§2) | 번호 ①~④ 는 지시서의 번호 |
| #space 제목 · 본문 · 「곧 문을 엽니다」 | 소유자 문안 (§2) | 주소 · 시간 · 개시일 없음 |
| #space 그림 alt | 2026-09-16 에 뺀 매장 섹션의 alt 에서 「키오스크」만 걷었다 | `b57940c^` |
| 메뉴 「진아젠」 · 「공간」 | 소유자 제안 (§3) | |
| 푸터 「회사소개」 | 옛 메뉴 글자 | |
| en · ja 전부 | 🔴 **초안** — `<!-- 초안 · 소유자 확인 — 원문: 「…」 -->` 주석 | |

**④ 「진아(眞我)」 한자 — 붙이지 않았다.** 소유자 문안에 없고, 두 저장소 어디에도 「眞我」가 없다(grep 0). 「진아」는 대표 이름(김진아)이기도 해서 한자를 붙이면 **브랜드 어원을 새로 주장**하게 된다. 붙이려면 소유자 확정 한 번 — 자리는 카드 제목 한 곳이다.

## §3 메뉴 — 판정 · 폭 실측

**여섯 그대로 · 차례는 DOM 순서:** 진아젠 `#philosophy` · 운명리포트 `/report/` · **공간 `#space` · 운세그래프 `#app`** · 매장 도입 `#store` · 문의하기 `#contact`. 회사소개는 푸터로.
- ⚠ 소유자 제안은 「운세그래프 · 공간」 차례였다. 이 저장소 메뉴는 「섹션 차례 그대로」가 규칙이라(SITE-36 주석) 공간이 앞이다. **한 칸 바꿈** — 되돌리려면 메뉴 두 줄
- ⚠ 「명상아티스트」가 메뉴에서 빠졌다(제안 그대로). 섹션은 남아 있다

**닻 높이로 잰 폭 (761px · 헤드리스 Chrome)** — 여섯 닻 높이가 모두 한 줄(23px)이고 넘침 0:

| | ko | en | ja |
|---|---|---|---|
| 여유 (main) | 47px | 12px | 9px |
| 여유 (story-1) | **77px** | **34px** | **46px** |

## 새 CSS 규칙 여섯 — 값의 출처

| 규칙 | 값 | 출처 |
|---|---|---|
| `.philo h1.line2` | `line-height:1.7` | `body` |
| `.hero h2` | 600 · `clamp(34px,5.2vw,60px)` · 1.2 · -.03em | 옛 `h1` 규칙 그대로 |
| `.hero h2 em` | normal · 800 | 옛 `h1 em` 그대로 |
| `.path .grid` (+ 820 · 720 두 폭) | 넷 칸 → 두 칸 → 한 칸 · 320px | 칸 수만 새 값. 820 은 `.feature` · 720/320 은 `.teas .grid` |
| `:lang(ko) .path .desc` | `keep-all` | `.usp p` · `.report p` 줄 그대로 |
| `:lang(ja) .path .desc` | `auto-phrase` | 같은 곳 |

틀은 전부 기존 규칙: #path = `.teas` · `.tea`(차 카드 · 비어 있던 `.role` 자리가 단계 이름), #space = `.store` · `.store-right` · `.stands`(2026-09-16 에 뺀 매장 섹션이 쓰던 거치대), #app = `.hero`.

## §5 확인

- 여섯 페이지 200 (로컬 서버) · h1 페이지마다 1 · alt 누락 0 · width/height 누락 0 · JSON-LD 파싱 셋 다 · 닻 목적지 없음 0
- `?mode=privacy` 반증 셋 — `?mode=privacy` → 방침 h1 · `?mode=PRIVACY` · `?mode=priv` · 없음 → 홈 h1. ⚠ en · ja 에는 이 스크립트가 **main 부터 없다**(이번 변경과 무관)
- 메뉴 761px 한 줄 (위 표) · 폰 400 가로 넘침 0 (세 언어) · 첫 화면 단추 둘 스크롤 없이 (세 언어 × 400 · 761 · 1280)
- 🔴 세 판매 경로 단추 — **바꾸기 전과 같은 수**: 시작하기 3 · 예시 2 · Play 1 · 매장 문의 1 (세 언어)
- 그림 site_01 · 05 · 06 은 `loading=lazy` · `decoding=async` · width/height. 히어로 그림 preload 는 걷었다(첫 화면 밖으로 내려갔다)
- web_report 가 홈페이지를 읽는 시험(`test_app_cta` · `test_legal_pages` · `test_design_tokens`) — **69 통과 · skip 0**
- ⚠ 검사기를 먼저 깨뜨린 표본(h1 둘 · alt 없음 · JSON-LD 깨짐 · 없는 닻 · Play 없음 · 첫 화면 단추 하나 · 매장 mailto 없음 · lazy 아님 · 메뉴 두 줄 · 첫 화면 단추 밖 · 가로 넘침)으로 돌렸다 — **전부 빨개졌다.** 🔴 그때 판정기가 **스타일이 안 실린 쪽을 통과시키는 구멍**이 있었다(여유 -730px 를 못 봄) → 「여유 음수」·「스타일 안 실림」을 실패로 더한 뒤 다시 돌렸다

## 발견 (고치지 않음)

- 대운 그래프 그림(site_01) 네 귀퉁이가 검게 찍힌다 — **main 에도 같다**(헤드리스 캡처 대조)
- 공간 섹션 그림 둘은 매장 **사진**이 아니라 매장 화면 **캡처**(v0.8.1 판 · 「운명리포트 출력하기 신청」 단추가 보인다)
- 흐름 카드의 단계 이름 높이가 카드마다 한 줄씩 다르다(한 줄 문장인 ③만 위로 올라간다)
- `docs/` 는 `_redirects` 가 301 로 막는다 — 갤러리는 **Pages 주소로는 안 열린다.** 로컬 파일로 연다
