# TASK-SITE-36-REPORT-LINK-1 — 홈페이지에 웹 리포트 섹션 + 메뉴

- 착수 2026-09-19 10:37 KST · 종료 2026-09-19 11:33 KST
- `main` · 착수 HEAD `e39c975` → `34d6221` · behind 0 · 추적 파일 변경 0
- 🔴 `privacy/` **변경 0** (Play 심사가 그 URL 을 본다)
- 🔴 푸터 사업자정보 여덟 항목 **변경 0** (카드사 심사가 본다)
- 🔴 DNS · Pages 설정 · Worker · Route **건드리지 않았다**
- 🔴 삭제된 줄 **0** — 순증 141줄뿐이다

---

## §0 지금 상태 — 일반인만 길이 없었다

| 판매 갈래 | 길 |
|---|---|
| 전문가 앱 | 히어로 Play 단추 ✅ |
| 매장 도입 | `#store` 섹션 + 메뉴 ✅ |
| **일반인 웹** | 🔴 **`/report` 로 가는 링크 0건** |

실측 — 세 본문 페이지에서 `href="…report…"` **0건**이었다.
⚠ 방침 세 장에 `jinazen.com/report` 라는 **글자**는 있으나 링크가 아니다(제14조 안내 문장).
🔴 그 문장은 건드리지 않았다.

### 🔴 폰 400px — 손님이 섹션에 닿기까지 몇 번 스크롤하나

| | 전 | 후 (배포본 실측) |
|---|---|---|
| ko | **닿을 수 없다** (링크·섹션 0건) | 섹션 top 1,530px → **스크롤 2회** |
| en | 닿을 수 없다 | 1,659px → **3회** |
| ja | 닿을 수 없다 | 1,597px → **2회** |

⚠ **0회가 아니다.** 히어로가 폰에서 1,500px 가까이 차지한다(문안 + 그래프 그림).
🔴 그래도 **히어로를 줄이지 않았다** — 「전문가를 위한」 문안과 LCP 그림은 앱 스토어·
Play 심사와 맞춰 둔 자리다(지시서 §0 의 「히어로를 고치지 않는다」).
🔴 그리고 폰에서는 `.menu{display:none}` 이라 **메뉴로는 아예 못 닿는다** — 스크롤이
유일한 길이고, 그래서 섹션을 히어로 **바로 다음**에 두었다. 더 위로 갈 자리가 없다.

⚠ en 이 한 번 더 걸리는 이유는 영어 히어로 문안이 길어서다. 이 판에서 손대지 않았다.

---

## §1 새 섹션 — 문안 출처 · 값 처리

자리: 히어로 **바로 다음** · `.usp`(소우주 띠) 앞. 매장 문의(`#store`)는 맨 아래 그대로.

### 🔴 새 문장을 한 줄도 짓지 않았다

| 자리 | 글자 | 출처 (jinazen 저장소 `web_report/`) |
|---|---|---|
| h2 | 진아젠 **운명리포트** | `api/pages.py` `ORDER_NAME` |
| 본문 | 서비스는 이용자가 입력한 … 디지털 콘텐츠입니다. | `flow_copy.SALES_INTRO` (= 약관 제4조 첫 문단 첫 문장) |
| 잔글씨 앞 | 한 번 결제하면 … 구독이 아닙니다. | `flow_copy.REOPEN_FREE` (= 약관 제11조 첫 문단 둘째 마디) |
| 잔글씨 뒤 | 가격은 결제 화면에서 확인해 주세요. | `legal.PRICE_LINE_FALLBACK` |
| 단추 ① | 예시 화면 보기 | `legal.SAMPLE_LINK_LABEL` |
| 단추 ② | 시작하기 | `flow_copy.START_LABEL` |
| 메뉴 라벨 | 운명리포트 | `flow_copy.REPORT_GROUP_LABEL` |

⚠ h2 를 「진아젠」 + 굵은 「운명리포트」로 가른 것은 **모양**이지 새 문안이 아니다 —
`.store` 의 「매장 <b>도입 문의</b>」가 같은 방식이다.

### 🔴 값 처리 — 「5,000원」을 글자로 박지 않았다

홈페이지는 정적이라 서버 값을 못 찍는데, **값은 관리 화면(`/report/admin/price`)에서
바뀐다.** 박아 두면 그때 두 곳이 갈리고 🔴 **갈려도 아무 일이 안 난다** — 아무 시험도
홈페이지를 안 본다.

`legal.PRICE_LINE_FALLBACK` 은 web_report 가 **「값을 못 읽었을 때」를 위해 이미 갖고
있는 문장**이다. 홈페이지는 언제나 그 처지이므로 그 문장을 그대로 쓴다.
⚠ 「한 번 결제 · 다시 보기 무료」의 뜻은 `REOPEN_FREE` 가 값 없이 말한다.

### 단추 둘

```
예시 화면 보기  → https://jinazen.com/report/sample   (채운 단추)
시작하기        → https://jinazen.com/report/
```

🔴 **채운 쪽이 「예시 화면 보기」다** — 구매 전 체험은 친절이 아니라 법적 조치이고
(전자상거래법 시행령 제21조의2 ③), web_report 설명 페이지도 그쪽을 채웠다. 두 곳이
같은 낱말·같은 차례다.

⚠ **주소를 절대경로로 적었다.** `/report` 는 같은 호스트이지만 Cloudflare **Worker
Route** 가 내는 자리이고 이 Pages 사이트가 아니다. 상대경로로 적으면 그 사정이 안 보인다.

### en · ja

초안 + `<!-- 초안 · 소유자 확인 — 원문: 「…」 -->` 주석을 줄마다 달았다.
🔴 **주소는 세 언어가 같다** — `/report` 는 지금 한국어만 낸다. 영어·일본어 화면을
약속하지 않으려고 링크 글자에도 언어를 적지 않았다.

---

## §2 메뉴 — 🔴 내가 넘치게 만들었고, 고쳤다

세 언어 메뉴에 항목 하나를 더했다(둘째 자리 — 섹션이 히어로 바로 뒤라 DOM 순서 그대로).

### 실측 (헤드리스 Chrome · `.nav .wrap` 안쪽 **705px** · 데스크톱 최소 폭 761px)

| | ko | en | ja |
|---|---|---|---|
| 다섯 항목 (이 판 전) | 144px | 55px | 84px |
| **여섯 항목 · 간격 26px** | 60px | 🔴 **-33px · 닻이 접혔다** | 🔴 **-1px · 접혔다** |
| 여섯 항목 · 간격 16px | 110px | 🔴 **0px · 접혔다** | 33px |
| **최종** (간격 16px + en 라벨 단축) | **110px** | **38px** | **33px** |

접히는 구간: en **761~855px** · ja **761px**. 856px 이상에서는 둘 다 멀쩡했다.

### ⚠ 이 고장은 눈에 잘 안 띈다 — 첫 측정이 틀렸다

`.menu` 가 `align-items` 를 안 주어 닻이 **늘어난다**(stretch). 그래서 닻 하나가
**자기 안에서 두 줄**이 돼도 **모든 닻의 `y` 가 그대로**다.
🔴 처음에 닻의 `y` 가 같은지로 재고 「한 줄 ✅」을 받았다 — **거짓일 수 없는 단언**이었다
(CLAUDE.md 함정 29). 닻 **높이**를 한 줄 높이와 대 보니 여섯 다 접혀 있었다.
⚠ 검출기가 멀쩡한지는 **HEAD 사본(다섯 항목)** 으로 확인했다 — 거기서는 접힘 0건이었다.

### 🔴 판정 — 라벨을 줄일 것인가

| 안 | 판정 |
|---|---|
| 간격만 줄인다 (26 → 16px) | ⚠ **모자란다.** en 이 761px 에서 0px |
| 간격을 더 줄인다 (12px) | 🔴 en 여백 **9px** — 글꼴이 바뀌면 다시 넘친다. 너무 아슬아슬하다 |
| **영어 초안 라벨만 짧게** (`Destiny Report` → `Report`) | 🟢 **채택** |

**사유** — ① 한국어 「운명리포트」는 web_report 확정 문안이라 내가 줄일 것이 아니다
② 나머지 다섯 라벨은 소유자 확정 문안이다 ③ 영어·일본어 라벨은 **초안**이고 소유자
확인 대기 중이라, 줄이는 비용이 가장 싸고 되돌리기도 쉽다.

⚠ **그래서 영어만 메뉴 라벨(`Report`)이 섹션 제목(`Destiny Report`)보다 짧다.**
ko·ja 는 제목의 굵은 마디와 같은 글자다. 🔴 소유자가 긴 쪽을 고르면 그때는 간격을 더
줄이거나 다른 라벨을 손봐야 한다 — 그 사정을 `en/index.html` 그 자리 주석에 적어 두었다.

⚠ 햄버거 메뉴는 이 TASK 밖이다(지시서). 폰에서 메뉴는 여전히 안 그려진다.

---

## §3 로컬 확인

🔴 **검사기를 일부러 깨뜨린 표본으로 먼저 시험했다** — 「전부 통과」는 검사기가
아무것도 안 봐도 나온다.

```
h1 둘 🟢  h1 없음 🟢  alt 없는 img 🟢  JSON-LD 깨짐 🟢
멀쩡한 표본 → 0건 🟢   (늘 빨개지는 검사도 아무것도 안 재는 것이다)
호스팅 세기 → 1 🟢
```

| 항목 | 결과 |
|---|---|
| 여섯 페이지 h1 = 1 · alt 누락 0 · JSON-LD 파싱 | 🟢 |
| 🔴 「호스팅 Cloudflare」 단독 건수 | **6 → 6 무변** |
| `/report` 링크 | 0 → **6** (3페이지 × 단추 2) · 섹션 3 · 메뉴 3 |
| 🔴 `?mode=privacy` → 방침 | 🟢 `/privacy/` · h1 「개인정보처리방침」 |
| 반증 `?mode=privacyX` · `?mode=Privacy` · 인자 없음 | 🟢 셋 다 홈에 머문다 |
| 폰 400px 가로 넘침 | 🟢 세 언어 0 |
| 단추 높이 | 🟢 54px |

⚠ 폰 400px 에서 한국어 본문이 「…제공하/고,」처럼 낱말 가운데서 끊겼다.
🔴 사이트가 `.usp` 에 이미 쓰는 규칙을 **같은 모양으로** 폈다 —
`:lang(ko) .report p{word-break:keep-all}` · `:lang(ja) … auto-phrase`.
(ja 에 `keep-all` 을 쓰지 않는 이유도 그 주석에 이미 적혀 있다.)

---

## §4 배포 후 확인 (10:52:21 push · **10:52:29 반영** — 8초)

배포 전 `/` 해시 `7af1e20d711c` · 16,943B → 배포 후 `851e1ed30b12` · 20,310B.

| 항목 | 결과 |
|---|---|
| 여섯 페이지 + `robots.txt` + `sitemap.xml` | 🟢 **전부 200** |
| `api.jinazen.com/health` | 🟢 200 |
| 🔴 「예시 화면 보기」를 **실제로 눌렀다** | 🟢 `/report/sample` 200 |
| 🔴 「시작하기」를 **실제로 눌렀다** | 🟢 `/report/` 200 · 제목 「진아젠 운명리포트」 |
| 🔴 Worker Route 가 그대로 사는가 | 🟢 `/report/` · `/report/sample` 해시가 **배포 전과 동일** |
| 🔴 `?mode=privacy` | 🟢 방침 · 반증 셋 전부 홈 |
| 🔴 방침 세 페이지 | 🟢 해시가 **배포 전과 동일** |
| 세 언어 섹션 · 메뉴 여섯 | 🟢 |

### ⚠ 배포본이 커밋보다 **+367바이트** — 내 변경이 아니다

처음 대조에서 여섯 페이지가 전부 367바이트씩 컸다. 🔴 **내가 안 건드린 방침 페이지도
똑같이 367바이트** 컸다 — 그것이 내 변경이 아니라는 가장 깨끗한 증거다.

정체는 Cloudflare **Web Analytics 비컨** 주입이다:

```
<script type="module" src="https://static.cloudflareinsights.com/beacon.min.js/…"
        data-cf-beacon='{"version":"2024.11.0","token":"…","r":1,"spa":2}' …></script>
```

비컨을 뺀 뒤 다시 대조 → 🟢 **여섯 페이지 바이트 단위 완전 일치.**

⚠ **주입이 매번 오지는 않는다** — 같은 URL 을 잠시 뒤 다시 받으니 비컨 없는 응답이 왔다.
🔴 그래서 「배포본 ↔ 커밋 바이트 일치」를 **한 번 재고 끝내면 안 된다**(이전
`TASK-SITE-CUTOVER-R1` 이 「완전 일치」로 적은 것도 비컨 없는 응답을 받은 것으로 보인다).

🔴 **보고만 한다 — 손대지 않았다.** Pages·Cloudflare 설정은 이 TASK 밖이다.
⚠ 다만 소유자 확인이 필요한 것이 하나 있다: **이 비컨이 개인정보처리방침 페이지에도
들어간다.** 방침이 제3자 분석 도구를 말하는지 대조해야 한다 — Play 심사가 그 페이지를 본다.

### 순증만인가

```
assets/style.css  +47 -0
index.html        +41 -0
en/index.html     +29 -0
ja/index.html     +24 -0
→ 삭제된 줄 0
```

---

## {{문안 대기}}

**없다.** 한국어는 전부 web_report 확정 문안에서 글자 그대로 왔다.
⚠ en · ja 는 **초안**이고 원문 주석이 줄마다 붙어 있다 — 소유자 확인 대상이다:

- h2 `JINAZEN Destiny Report` · `JINAZEN 運命リポート`
- 본문 · 잔글씨 각 한 벌
- 단추 `See a sample` / `Get started` · `サンプルを見る` / `はじめる`
- 🔴 메뉴 `Report` — **섹션 제목보다 짧다**(§2 판정)

---

## 미검증 · 남은 것

- 🔴 **실기기 확인 없음.** 폰·메뉴 수치는 전부 헤드리스 Chrome 실측이다
- ⚠ `sitemap.xml` 에 `/report` 를 넣지 않았다 — 그 주소는 이 Pages 사이트가 아니라
  **Worker 가 내는 자리**이고, web_report 는 쪽마다 `noindex` 를 붙인다. 섹션은 기존
  페이지의 앵커라 새 URL 도 아니다
- ⚠ 「예시 화면 보기」로 간 `/report/sample` 의 **브라우저 탭 제목이 「진아젠 운세그래프」**다
  (앱 이름). 운명리포트 화면인데 제목이 앱을 가리킨다 — web_report 쪽 일이라 이 TASK 에서
  고치지 않았다
- ⚠ **en 은 섹션까지 스크롤 3회**다. 영어 히어로 문안이 길기 때문이고 이 판에서 손대지 않았다
- 🔴 Cloudflare 비컨이 방침 페이지에 주입되는 건(위 §4) — 소유자 확인 대상
