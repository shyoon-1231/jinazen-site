# TASK-SITE-41-SPACE-1 — 공간 섹션 정정 · 한방차 이스트프렌즈 링크 (story-1 이어서)

- 착수 2026-09-22 20:28 KST · 종료는 보고에 실제 시각으로 · 🔴 브랜치 `story-1` (착수 HEAD `f2ab123` · behind 0 · 추적 파일 변경 0)
- 🔴 **main 에 합치지 않았다**
- 🔴 `privacy/` · 푸터 사업자정보 · DNS · Pages · Worker · Route · `_redirects` 변경 0 · `style.css` 토큰 변경 0 · 지운 줄 0

## §0 정정 — 뺀 자리

소유자 정정(2026-09-22): 속초 공간은 **상담하는 곳이 아니다 — 스스로 자신을 찾는 곳이다.**

| 언어 | 뺀 것 |
|---|---|
| ko | 제목 「속초의 명상·상담 공간」 · 둘째 문장 「전문 상담사가 사주·자미두수·타로운명수로 당신의 흐름을 함께 읽습니다.」 |
| en | 「consultation space」 · 「Professional counselors read your flow …」 · 원문 주석 |
| ja | 「瞑想・相談スペース」 · 「専門の相談師が …」 · 원문 주석 |

공간 구역(`<!-- ── 5 공간` 주석부터 `<!-- ── 6 전문가 앱` 주석 앞까지 · 본문 · 카드 · 주석 · alt)에 **「상담 · 相談 · consult · counsel」 0건** — 세 언어. 검사기에 넣었다.
매장 도입 섹션의 「상담」은 별개라 그대로다.

## §1 공간 — 소유자 문안 그대로

- 제목 「속초, **진아젠의 공간**」(굵기만 띠 모양) · 부제 「진아젠의 철학을 몸으로 체험하는 곳」 · 본문 문단 넷 · 「곧 문을 엽니다」
- 🔴 주소 · 시간 · 개시일 없음. 「영랑해변」은 문안 안의 글자다
- ⚠ 문안의 줄바꿈 둘은 `<br>` 로 옮기지 않았다 — 문장 한가운데라 460px 칸에서 두 번 꺾였다(실측). 글자는 그대로
- **체험 카드 넷** — #path 와 같은 틀(`.teas` · `.path` · `.tea`) · 번호 ①~④ · ② 에 「이스트프렌즈에서 보기 ↗」
  - 자리: 짙은 공간 띠(`.store`) **바로 뒤 밝은 띠**(`.alt`). 짙은 띠의 두 칸 격자 안에 넣으면 격자 규칙을 새로 적어야 한다
- en · ja 초안 + 원문 주석. 「황금부리」는 기존 번역어 그대로(en 「Golden Beak」 · ja 「黄金のくちばし」)

### 🔴 「키오스크」 판정

2026-09-16(SITE-PREP-2)에 사이트에서 뺀 낱말이다. **이번에는 소유자 문안에 있으므로 쓴다.**
- 그래서 그림 alt 도 그때(`b57940c^`) 글자 **그대로** 되돌렸다(SITE-40 은 「키오스크」만 걷었었다)
- 홈페이지에는 이 낱말을 막는 **자동 검사가 없다** — 두 저장소를 찾았다(web_report 시험의 등장은 앱 코드 이야기). 등록할 예외가 없으므로 판정은 이 기록과 공간 구역 주석에 남긴다
- ⚠ 옛 매장 섹션 주석(「키오스크 캡처 2장 … 되살리지 않은 것」)은 이력이라 그대로 둔다

## §2 한방차 → 이스트프렌즈

- `https://eastfriends.net/` — **200 · 넘김 0** (UA 없이도 · 제목 「이스트프렌즈 | 황금부리 · 김진아 명상 루틴 · 자미차,쌍화차,천일차,천월차 공식몰」)
- 자리 둘 × 세 언어: ② 카드 · 차 섹션(#teas) 아래 `.cta` 단추
- 🔴 **새 창 판정** — 이 사이트의 외부 링크는 지금까지 **Play 하나**이고 **같은 창**(target 없음)이다. 관례는 「같은 창」이지만 **지시(새 창)를 따랐다**:
  - Play 는 **우리 앱의 설치 자리**다 — 폰에서는 Play 앱이 열려 창 개념이 없고, 손님이 할 일이 거기서 끝난다
  - 이스트프렌즈는 **다른 사업자의 쇼핑몰**이다 — 홈페이지 읽기를 이어 가야 하므로 돌아올 창을 남긴다
  - 그래서 둘을 **맞추지 않는다.** Play 는 그대로 두었다
- 모든 `target="_blank"` 에 `rel="noopener noreferrer"` — 검사기가 잰다
- 카드 안 링크 색 — 전역 `a{color:inherit;text-decoration:none}` 라 글자와 구별되지 않아 `.ch .k` 의 값(`--gold-ink` · 600)을 썼다

## 새 CSS 규칙 셋 — 값의 출처

| 규칙 | 값 | 출처 |
|---|---|---|
| `.space-cards .role a` | `--gold-ink` · 600 | `.ch .k` |
| `:lang(ko) .space p` | `keep-all` | `.usp p` · `.report p` · `.path .desc` 줄 |
| `:lang(ja) .space p` | `auto-phrase` | 같은 곳 |

## §3 확인

- 여섯 페이지 200 · h1 페이지마다 1 · alt 누락 0 · JSON-LD · 닻 목적지 · 메뉴 761px 한 줄 · 폰 400 가로 넘침 0 · 첫 화면 단추 둘 스크롤 없이 — SITE-40 과 같은 검사기 · **전부 통과**
- 🔴 세 판매 경로 단추 **그대로**: 시작하기 3 · 예시 2 · Play 1 · 매장 문의 1 (세 언어)
- 🔴 공간 구역 상담 낱말 0 (세 언어) · 이스트프렌즈 링크 페이지마다 2 · 둘 다 새 창 · rel 있음
- ⚠ 새 검사 둘을 먼저 **깨뜨린 표본**으로 돌렸다 — 착수 HEAD(상담 든 판) · 공간에 「상담」 심기 · rel 뺀 새 창 링크 → **전부 빨개졌다**
- web_report 대조 시험(`test_app_cta` · `test_legal_pages` · `test_design_tokens`) **69 통과 · skip 0**
- 갤러리 `docs/screenshots/story-1/index.html` — 공간 · 차 조각을 더하고 첫 화면 · 전체를 새로 떴다

## 미검증

- 링크를 **실제로 눌러** 새 창으로 도착하는 것 — 헤드리스로는 새 창을 따라가지 못했다. href · target · rel 과 그 주소의 200 까지만 쟀다
- 실기기(폰 · Safari) · Lighthouse
