# TASK-SITE-38-REPORT-ENTRY-1 — 홈페이지에서 운명리포트로 가는 길을 크게

- 착수 2026-09-22 02:33 KST · 종료는 push 뒤 확인까지 마친 실제 시각으로 보고에 적는다
- `main` · 착수 HEAD `8ba4e36` · behind 0 · 추적 파일 변경 0
- 🔴 `privacy/` **변경 0** · 푸터 사업자정보 여덟 항목 **변경 0** · DNS · Pages · Worker · Route **안 건드렸다**
- 🔴 새 문안 0 — ko 는 web_report 확정 글자, en·ja 는 SITE-36 초안 글자 그대로 · 가격 글자 0

## 🔴 제약이 풀렸다 — 기록

SITE-36 은 「히어로를 고치지 않는다」 아래서 했다(Play 심사 때문). **Play 심사가 끝나 그 제약은 풀렸다**
(소유자 확인 2026-09-22). 이 판은 그 위에서 첫 화면을 손댔다.

## §0 실측 (라이브 · 헤드리스 Chrome · 브라우저 UA)

| | 결과 |
|---|---|
| ① 첫 화면(히어로)에 운명리포트 단추 | 🔴 **없다** — 히어로 안 `/report` 링크 0 · 첫 화면(400×800 · 1280×800)에 보이는 리포트 링크 0 |
| ② 메뉴 「운명리포트」 | `#report` — 섹션으로 스크롤 |
| ③ 섹션까지 스크롤 (섹션 top ÷ 화면 높이 800 올림) | 폰 400: ko **2**(1,530px) · en **3**(1,659) · ja **2**(1,597) — SITE-36 과 같다. PC 1280: 세 언어 **3** |

⚠ **첫 화면은 히어로가 아니라 사진 띠(`.philo`)다.** 히어로 제목은 그 아래에 있고, 히어로의 Play 단추는 두 폭 다
첫 화면 **밖**이다.

## §1 단추 둘 — 사진 띠 안

- 자리: 「진아젠 JINAZEN」 바로 아래. 🔴 **스크롤 없이 보이는 유일한 자리**다(폰 단추 아래끝 408px · PC 464px / 화면 800)
- `시작하기`(채움 · 주) → `https://jinazen.com/report/` · `예시 화면 보기`(보조) → `https://jinazen.com/report/sample`
- en `Get started` / `See a sample` · ja `はじめる` / `サンプルを見る` — SITE-36 초안 그대로
- ⚠ **주 단추가 「시작하기」인 것은 소유자 지시다.** 바로 아래 운명리포트 섹션과 web_report 설명 페이지는
  「예시 화면 보기」가 채운 쪽이다(WEB-35 · 구매 전 체험 법적 조치). 🔴 **한 페이지 안에서 채운 쪽이 갈린다** —
  소유자 판단 대상으로 남긴다
- 🔴 **대비** — 처음 둔 「속 빈 흰 테두리」(`.store .cta` 값)는 사진 위 흰 글자 대비가 폰에서 **평균 4.2:1 ·
  밝은 5% 경계 3.1:1 · 가장 밝은 점 1.6:1** 이었다(AA 4.5:1 미달). → 보조 단추를 **흰 바탕 · 잉크 글자**
  (`.store .cta:hover` 값)로 바꿨다. 두 단추 모두 **18.9:1** · 사진과 무관
- 🔴 **앱(Play) 단추는 지우지 않았다** — 히어로에 그대로다. 판정: 일반인은 첫 화면(사진 띠), 전문가 앱은 그다음(히어로).
  히어로 문안 「전문가를 위한」이 앱 단추의 자리를 이미 말한다

## §2 메뉴 — 목적지로 바로 (권고 채택)

`#report` → `https://jinazen.com/report/` (세 언어). 섹션은 첫 화면 단추와 스크롤로 닿는다.
⚠ 폰에는 메뉴가 없다(760px 이하 `display:none`) — 폰의 입구는 §1 이다.

## §3 섹션 — 제목 한 단계 크게 · 카드 넷은 넣지 않았다

- 섹션 h2 를 홈페이지 h1 단계(`clamp(34px,5.2vw,60px)`)로. 여백은 이미 `section.pad` 104px
- 🔴 **카드 넷을 넣지 않았다** — ① en·ja 글자가 없다(ko 는 web_report 에 있으나 en·ja 는 새 초안을 지어야 한다 —
  금지) ② 같은 카드가 한 번 누르면 닿는 `/report/` 에 이미 있다 ③ 두 곳에 두면 **홈페이지를 재는 시험이 없어**
  갈려도 아무 일이 안 난다. 샘플 캡처 그림도 넣지 않았다(지시)

## §4 로컬 확인 (python http.server · 헤드리스 Chrome · 브라우저 UA)

- 🔴 검사기 자체 시험: 깨진 표본 4건(h1 둘 · h1 없음 · alt 없음 · JSON-LD 깨짐) 전부 잡음 · 멀쩡한 표본 0건
- 여섯 페이지 200 · h1 각 1 · alt 누락 0 · JSON-LD 파싱 🟢
- 🔴 첫 화면 단추 둘: 세 언어 × 400·1280 **여섯 경우 전부** 스크롤 없이 보인다
- 🔴 `?mode=privacy` → `/privacy/` · 반증 `?mode=privacyX` · `?mode=Privacy` · 인자 없음 → 홈에 머문다
- 🔴 메뉴 761 · 800 · 900px 세 언어 — 닻 높이 전부 23px(한 줄) · 접힘 0. 검출기는 **간격을 60px 로 벌린 표본**에서
  접힘 6개를 잡는 것을 먼저 확인했다
- 폰 400 가로 넘침: 여섯 페이지 0

## {{문안 대기}}

- 없다(ko). ⚠ en·ja 는 SITE-36 초안 그대로다 — 소유자 확인 대상은 SITE-36 목록과 같다
