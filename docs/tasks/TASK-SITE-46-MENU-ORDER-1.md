# TASK-SITE-46-MENU-ORDER-1 — 메뉴 차례 · 배너 화살표 겹침

- 착수 **2026-09-23 09:28 KST** · 종료는 보고에 · `main` · 착수 HEAD `70a39bc` · 추적 변경 0
- 🔴 `privacy/` · 사업자정보 · DNS · Pages · Worker · Route 변경 **0** · `style.css` 토큰 변경 **0**
- 바꾼 파일 넷: `index.html` · `en/index.html` · `ja/index.html` · `assets/style.css` (+31 / −9)

## §1 메뉴 차례 (소유자 확정 2026-09-23)

```
옛: 진아젠 · 개인용 · 공간 · 전문가용 · 매장 도입 · 문의하기
새: 진아젠 · 개인용 · 전문가용 · 공간 · 매장 도입 · 문의하기
```

세 언어 모두. **라벨은 한 글자도 안 바꿨다** — 줄만 옮겼다.

⚠ **섹션 차례는 그대로다**(개인용 → 공간 → 전문가용). 메뉴와 페이지가 **일부러 어긋난다** —
메뉴는 「무엇을 파는가」를 나란히 보이고, 페이지는 「읽는 차례」로 둔다.

🔴 SITE-40 §3 은 「메뉴는 섹션 차례대로」라는 저장소 관례를 따라 소유자 제안을 한 칸 바꿨었다.
이번 확정이 그 판정을 **뒤집었다.** 🔴 그 주석을 지우지 않고 **취소선으로 남겼다** —
왜 한 번 바꿨다가 되돌렸는지가 남지 않으면 다음 사람이 같은 조사를 다시 한다.

### 🔴 761px 한 줄 — 닻 높이로 쟀다

| 언어 | 줄 수 | 메뉴 폭 | 남는 자리(브랜드↔메뉴 / 메뉴↔언어) |
|---|---|---|---|
| ko | **1** | 316.8px | **101.5 / 101.5** |
| ja | **1** | 385.4px | **67 / 67** |
| en | **1** | 435.1px | 🔴 **42.5 / 42.5** — 가장 빠듯하다 |

⚠ 메뉴 링크의 `top` 값이 **하나**인 것으로 줄 수를 셌다(라벨 글자 수가 아니라 실제 배치).
🔴 **en 이 42.5px 밖에 안 남는다** — 라벨을 한 글자라도 늘리면 761px 에서 두 줄이 된다.
(820px 에서는 72 / 72 · 폰 760 이하에서는 메뉴가 아예 안 그려진다 — 종전 그대로)

## §2 배너 화살표 겹침 — 🔴 범위가 기록보다 넓었다

SITE-45 는 「721~820px」로 적어 두었다. **실측하니 721~1183px 전 구간이었다**
(ko·en·ja × 열세 폭 · 배너 ②의 본문 첫 세 글자 사각형과 화살표 사각형을 겹쳐 봤다):

| 폭 | 겹치는 곳 |
|---|---|
| 700 · 710 · 720 | 0곳 (화살표가 안 보인다 — 종전 규칙 `max-width:720px`) |
| **721 ~ 860** | ko② · ja② |
| **920 ~ 1120** | ko② · en② · ja② |
| **1200 · 1280** | 0곳 |

🔴 **1184px 부터 저절로 사라지는 까닭** — `.wrap`(max 1120)이 가운데로 모이면서 본문 왼끝이
화살표 오른끝(60px) 밖으로 나간다. 그 아래에서는 본문이 늘 `28px`에서 시작해 화살표(16~60)와 겹친다.
⚠ en 이 좁은 폭에서 안 걸린 것은 글줄이 다르게 접혀 **세로로** 안 겹쳤을 뿐이다 — 가로는 늘 겹쳤다.

### 🔴 고른 방법 — 숨기지 않고 **본문을 화살표 바깥으로 밀었다**

```css
@media(min-width:721px){.banners .banner>.wrap{padding-left:72px;padding-right:72px}}
```

- **왜 숨기지 않았나** — 겹침이 721~1183 이므로 「그 폭에서 숨긴다」는 사실상 **거의 모든 화면에서
  화살표를 없애는** 것이 된다. 점 셋과 밀기는 남지만, 화살표를 쓰는 사람에게는 통로가 준다
- **새 값 0** — `72 = 16 + 44 + 12`
  (`.banner-arrow.prev{left:16px}` · 화살표 44px · `.banner .acts{gap:12px}` · 전부 이 파일 안 기존 값)
- ⚠ **720px 이하에는 걸지 않았다** — 거기서는 화살표가 없으니 밀 까닭이 없다(폰 여백 그대로)
- ⚠ **대가** — 721px 이상에서 배너 본문 칸이 좌우 **각 44px**(합 88px) 좁아진다. 겹침이 없던
  1184px 이상에도 같이 걸린다. 🔴 폭마다 여백이 갈리면 그 경계에서 글줄이 **툭 바뀌므로** 한 값으로 뒀다

**고친 뒤 실측 — 겹침 0곳** (ko·en·ja × 721·761·820·860·900·960·1000·1100·1183·1280)

## §3 확인 · 배포

### 로컬

| 확인 | 결과 |
|---|---|
| 점 셋 · 멈춤 단추 | 🟢 세 언어 × 360·400·761·820·1280 **전부 점 3 · 멈춤 있음** |
| 폰 첫 화면 점 셋 | 🟢 400×800 점 바닥 **546.4px** (SITE-45 실측 546 과 같다) |
| 가로 넘침 | 🟢 **0** (열다섯 곳 전부) |
| 판매 단추 수 | 🟢 세 언어 **동일** — 시작하기(/report/) **3** · 예시 **2** · Play **2** · 매장 문의 mailto **1** |
| web_report 대조 시험 | 🟢 **116 통과 · skip 0** (SITE-45 와 같은 수) |

### 배포 (Pages 자동)

- push **10:04:14 KST** → 라이브 반영 **10:04:44** (30초)
- 🔴 라이브 = 커밋 **바이트 일치**(비컨 없는 응답): `/` `1d1dbe83…` · `/en/` `85c55d43…` · `/ja/` `5b413fc6…`

| 지문(비컨 걷고 **3회씩**) | 배포 전 | 배포 후 |
|---|---|---|
| `/` · `/en/` · `/ja/` | cde27f50 · cc965944 · 03050e38 | **바뀜** 59944ae2 · fab2a54f · 78ab8a10 |
| 🔴 `/privacy/` · `/privacy/en/` · `/privacy/ja/` | 8146ea20 · d441aef0 · 7088ab58 | 🟢 **같음** |
| 🔴 `robots.txt` · `sitemap.xml` | 2b643aeb · e9538494 | 🟢 **같음** |

같은 쪽은 3회 모두 같은 값이었다.
⚠ **비컨은 `Accept: text/html` 일 때만 주입됐다**(붙이지 않으면 아예 없다). 지문 스크립트는 그 `<script>` 만
걷으므로 빈 줄이 하나 남아 **바이트 대조용 해시와는 값이 다르다** — 전·후는 **같은 방식끼리** 비교했다.

### 라이브 (브라우저)

| 확인 | 결과 |
|---|---|
| `?mode=privacy` | 🟢 `https://jinazen.com/privacy/` · h1 **개인정보처리방침** |
| 반증 셋 — `?mode=PRIVACY` · `?mode=priv` · 인자 없음 | 🟢 **셋 다 홈에 머묾** (h1 진아젠 JINAZEN) |
| 메뉴 차례 | 🟢 세 언어 모두 새 차례 · 761~1280 **한 줄** |
| 🔴 화살표 겹침 | 🟢 ko·en·ja × 721·761·820·900·1100·1280 **0곳** |

⚠ `?mode=privacy` 는 `index.html` 의 **스크립트**가 한다(`location.replace`) — `curl` 로는 200·홈으로 보인다.
🔴 그래서 브라우저로 재야 한다. 종전 기록도 그렇게 잰 값이다.

## 미검증

- 실제 손가락으로 미는 동작(스크롤 스냅) — 브라우저 자동화로만 확인했다
- 배너 본문 칸이 88px 좁아진 뒤의 **글줄 접힘**을 사람 눈으로 본 것 — 수치(넘침 0 · 점 위치)만 확인했다
- 1184px 이상에서 여백을 그대로 둔 판단 — 보기에 어떤지는 소유자 확인이 필요하다
- 화살표를 **세로로** 옮기는 안(점 줄 옆 등)은 재지 않았다 — 이번엔 여백 한 줄로 끝냈다
