# TASK-SITE-55-LOGIN-FLICKER-1 — 머리띠 「로그인 → 마이페이지」 바뀌는 순간 줄이기

- 착수 **2026-09-25 16:01 KST** · 종료 **16:21 KST** (이 기록 커밋 직전) · `main` · 착수 HEAD `f812484`(SITE-54 끝 · 라이브 = 로컬) · behind 0 · 추적 변경 0
- 사본(되돌리기용) — `…/scratchpad/site55-bak/` · 🔴 **git 으로 되돌린 적 0 · revert 0**
- 커밋 둘 — `14ec6ad` · 이 기록
- 🔴 `privacy/` · 사업자정보 · DNS · Pages · Worker · Route 변경 **0** · `:root` 토큰 변경 **0** · 네이버 메타 변경 **0줄**

## 무엇을 했나

- **기억** — `localStorage` 열쇠 하나 `jz_login_hint` = `"1"` | `"0"`. 그 밖의 값은 쓰지 않는다(회원번호 · 이름 · 시각 0)
- **첫 그림** — 머리(head) 안 **인라인 한 줄**(네 쪽 같은 글자)이 기억을 읽어 `<html>` 에 표시를 붙인다:
  `"1"` → `jz-in`(처음부터 「마이페이지」) · `"0"` → 없음(「로그인」) · 없음 · 모르는 값 → `jz-wait`(글자 숨김 · 600ms 뒤 스스로 푼다)
  🔴 머리에 둔 까닭 — `account.js` 는 `defer` 라 첫 그림보다 늦고, 느린 망에서는 파일 자체가 늦게 온다.
  **600ms 상한을 지키려면 타이머가 인라인에 있어야 한다**(account.js 1.5초 늦춤 표본으로 확인)
- **답이 오면** (`account.js`) — 기억과 같으면 그대로 · 다르면 그때만 바꾸고 기억을 고친다.
  답이 아닌 것(실패 · 2초 초과 · 모양 다름)은 기억을 **안 고친다** — 2초 시간 초과는 그대로
- **저장소 막힘** — 인라인 · account.js 모두 `try` — 예외 없이 SITE-50 동작
- **스크립트 꺼짐** — 표시가 안 붙어 숨김 규칙이 안 걸린다 — 「로그인」 그대로
- **⑤ 로그아웃 뒤** — 기억 "1" 로 「마이페이지」가 잠깐 보일 수 있다 → 답(거짓)이 오면 「로그인」 · 기억 "0".
  🟢 해가 없다 — 비로그인이 `/mypage/` 를 열면 **로그인 화면**이 나온다(2026-09-25 라이브 실측: 401 · 제목 「로그인 — 개인용 운세그래프」)
- CSS 새 규칙 둘(`html.jz-in` · `html.jz-wait`) · 새 값 0 · 토큰 0

## 재현 (ko · en · ja · zh 모두 같은 결과 · 시각은 탐색 시작부터)

| 경우 | 보인 글자 차례 | 시각(ko) |
|---|---|---|
| 기억 1 + 답 true | 「마이페이지」 **하나 · 바뀜 0회** | — |
| 기억 없음 + 답 true 300ms | 숨김 → 「마이페이지」 한 번 | 54ms → 412ms |
| 기억 없음 + 답 true 1s | 숨김 → 「로그인」 → 「마이페이지」 | 621ms · 1121ms |
| 기억 없음 + account.js 1.5s 늦음 | 숨김 → 「로그인」 | 615ms (파일 도착 전) |
| 기억 1 + 답 false (로그아웃 뒤) | 「마이페이지」 → 「로그인」 · 기억 0 | — |
| 저장소 막힘 + 답 true 300ms | 「로그인」 → 「마이페이지」 (SITE-50 동작 · 페이지 오류 0) | — |

그 밖에: 기억 0 + true · 기억 1 + 500 · 답 없음(매달림) · 모양 다름 · 모르는 기억 값 · 저장소 막힘 + false · 스크립트 꺼짐 · 폰 400.
칸 폭 · 자리 — 숨김 · 로그인 · 마이페이지 **세 상태 같음**(네 언어 × 761 · 1280 · 글꼴이 다 온 뒤).

## 확인

- 🔴 **변이 먼저** — 다섯 전부 빨개졌다: 기억 무시(ko) · 숨김 규칙 빼기 · 저장소 예외(en · `try` 뺌 → 페이지 오류) ·
  스크립트 꺼짐에 숨김이 걸림(ja · html 에 jz-wait 를 박음) · 600ms 타이머를 account.js 에만(zh)
- ⚠ **동시에 다섯을 돌렸을 때 변이 안 넣은 ko 의 「기억 없음 + 300ms」가 한 번씩 빨개졌다** — 답 300ms + 처리가 600ms 를
  넘어 「로그인」이 잠깐 보인 것(설계대로의 동작 · 기계 부하). 혼자 돌리면 변이를 넣은 쪽만 빨갛고, 실제 작업본은 세 번 **80/0**
- 깜박임 검사기 **80 통과 · 실패 0** · SITE-50 머리띠 검사기 **88 통과 · 실패 0** · 사이트 검사기 **294 통과 · 실패 0**
- 메뉴 폭 **그대로**(en 761 **9.8**) · 판매 단추 그대로 · 주석 여닫는 수 ko **49/49** · en **66/66** · ja **66/66** · zh **67/67**

## 배포 전 지문 (비컨 걷고 3회 · 아홉 주소 3/3)

`/` `cea99e88` · `/en/` `ae20d9ae` · `/ja/` `45e51675` · `/zh/` `bea831ee` ·
🔴 `privacy/` 셋 `00bab904`·`1421d16f`·`b8e67629` · `robots` `84da6283` · `sitemap` `6d98deeb`

## 미검증 · 소유자 확인

- 🔴 **실제 로그인한 브라우저** — 두 번째 방문부터 「마이페이지」가 바로 보이는지(첫 방문은 숨김 → 글자)
- ⚠ 첫 방문 손님(기억 없음)은 최대 600ms 동안 그 칸이 비어 보인다 — 지시서가 고른 동작
