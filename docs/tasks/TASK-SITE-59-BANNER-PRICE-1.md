# TASK-SITE-59-BANNER-PRICE-1 — 배너 ② 가격 줄도 숨김 · 깜박임 검사기 흔들림 가리기

- 착수 **2026-09-26 08:34 KST** · 종료 **10:48 KST** (이 기록 커밋 직전) · `main` · 착수 HEAD `32fd25c`(SITE-58 끝 · 라이브 = 로컬) · behind 0 · 추적 변경 0
- 사본(되돌리기용) — `/private/tmp/claude-502/-Users-sh-yoon-dev-jinazen/ecd28bd2-de62-4b52-9b20-f914cc426b5c/scratchpad/site59-bak/` · 변이 사본 `…/mut59/P1.bak` · `H1.bak` · 🔴 **git 으로 되돌린 적 0**
- 커밋 둘 — `793f204`(§1) · 이 기록
- 🔴 `privacy/` · 사업자정보 · DNS · Pages · Worker · Route · `:root` 토큰 · 네이버 메타 · 머리띠 로그인(`account.js` · 머리 인라인) **무변경**(사본과 대조) · `price.js` 는 **주석 2줄만**

## §1 배너 ② 가격 줄 (`793f204`)

- `p.price-line` 에 `data-price-when` — 섹션 문장(SITE-58)과 **같은 표시**. CSS 는 규칙 **하나**로 넓혔다:
  ~~`.report p.price-say[data-price-when]:not(.is-priced){display:none}`~~ → `p[data-price-when]:not(.is-priced){display:none}`
  🔴 채우는 함수는 `price.js` 의 `fill` 하나 그대로(코드 무변경) — 두 벌 0
- 503 · 2초 초과 · 모양 다름 · 스크립트 꺼짐 → 가격 줄 없이 「구매하기」만

### 🔴 판정 — 줄이 나타날 때 배너 높이가 바뀌는가: **안 바뀐다**

| 폭 | 360 | 400 | 761 | 820 | 1024 | 1280 |
|---|---|---|---|---|---|---|
| 배너 줄 높이(ko · 채움 = 503 = 꺼짐) | 514.5 | 492.4 | 492.4 | 492.7 | 504.8 | 504.8 |

네 언어 × 여섯 폭 전부 **채움 = 503 = 스크립트 꺼짐**이고, 아래 철학 섹션 위치도 채움 = 503 이다.
까닭 — 배너 셋이 한 줄(track)에 서고 높이는 가장 큰 배너가 정한다. ② 는 그 안에서 가운데 맞춤이라 글자 묶음만 움직인다.
⚠ 스크립트가 꺼지면 철학이 44px 위다 — 점 셋 줄(`banner.js` 가 만든다)이 없어서이고 가격 줄과 무관하다(그래서 아래 섹션 대조는 채움 ↔ 503 만 쓴다).

## §2 깜박임 검사기(머리띠) 흔들림

### ① 스무 번 — 코드 그대로 (08:48 ~ 09:36)

| 결과 | 회수 |
|---|---|
| 실패 0 | **17** |
| 실패 1 | 2 (1회 · 8회) |
| 실패 3 | 1 (2회 · 부하 26.7) |

실패 **5건 전부** — 1280 폭 · 「300ms 답」 두 항목 · 글자 `['', 'o', 'i']`(숨김 → 로그인 → 마이페이지):

| 회 | 시각 | 부하 | 언어 | 항목 |
|---|---|---|---|---|
| 1 | 08:48~08:51 | 10.8 | zh | 모르는 기억 값 x + 답 true(300ms) |
| 2 | 08:51~08:53 | 26.7 | ko | 기억 없음 + 답 true 300ms |
| 2 | 08:51~08:53 | 26.7 | ko | 모르는 기억 값 x + 답 true(300ms) |
| 2 | 08:51~08:53 | 26.7 | zh | 모르는 기억 값 x + 답 true(300ms) |
| 8 | 09:05~09:07 | 2.1 | zh | 모르는 기억 값 x + 답 true(300ms) |

### ② 알려진 경계 외의 실패 — **없다**

「모르는 기억 값 x」도 **같은 경계**다 — 기억이 없거나 모르는 값이면 둘 다 숨김(`jz-wait`) · 600ms 타이머로 가고, 답은 300ms 늦춤이다.
SITE-58 첫 회 4건도 이 둘이었을 것으로 보이나 **그 기록은 없다**(추정).

### ③ 제품 결함인가 — **아니다 · 검사기가 시간에 기대 흔들린다**

- 평소 답 도착은 **약 400ms**(fetch 는 문서를 다 읽은 뒤 시작 + 300ms 늦춤) — 600ms 타이머까지 여유가 **150~200ms** 뿐이다
- 가짜 응답 처리기가 `time.sleep` 으로 늦추는데, 동기 Playwright 에서는 그동안 **다른 파일 응답이 전부 멈춘다** — 부하가 오면 답이 600ms 를 넘는다
- 답이 600ms 뒤에 오면 「숨김 → 로그인 → 마이페이지」는 **설계대로의 동작**이다(SITE-55 「기억 없음 + 답 1s」 항목이 그것을 따로 잰다)
- 🔴 고친 판이 **전제가 선 회**에서 이 항목을 재면 늘 초록이다 — 전제가 선 회에서 빨간 적이 없다(아래)

### 고친 것 — 검사기(`scratchpad/tools/check_hint2.py`) · 🔴 기준 무변경

- 시간에 기대는 항목 넷(300ms 둘 · 1초 · account.js 늦음)만 **전제**를 먼저 잰다:
  **기준 타이머**(문서 시작에 건 600ms 가 100ms 넘게 늦게 울렸나 = 메인 스레드가 바빴나) ·
  **답 도착**(300ms 항목은 `/report/auth/me` 응답 끝이 550ms 이하였나 — 자원 시각 표)
- 전제가 안 선 회만 **다시 잰다**(최대 5회 · 이유와 그 회 글자를 적는다). 🔴 전제가 섰는데 틀리면 **그대로 실패** · 5회 다 안 서면 **실패**(「재지 못했다」)
- 🔴 **진짜 결함은 여전히 잡는다** — 제품 변이 H1(ko 머리 인라인 타이머 600 → 100ms) → **3 빨강 · 다시 잰 회 0**(전제가 선 채로 틀렸다)

### 고친 뒤 스무 번

09:41 ~ 10:31 · **20회 전부 실패 0**(회마다 96 통과 = 원래 80 + 판정 기록 16). 부하가 저절로 **1.9 ~ 42.2** 사이를 오갔다.
전제가 안 서 다시 잰 것 — **2회에서 7번**(회 9 · 회 11 · 이유는 모두 「답 도착 > 550ms」 · 553 ~ 793ms).
🔴 그중 답이 793ms 에 온 한 번은 글자가 `['', 'ログイン', 'マイページ']` 였다 — 옛 판이면 **빨강**이었을 회다. 다시 재니 전제가 섰고 초록이었다.

## 확인

- 🔴 **변이** — P1 배너 가격 줄을 못 읽을 때 `visibility:hidden`(빈 줄 보이기) → **48 빨강** · H1(위) → **3 빨강** · 되돌린 뒤 `cmp` 같음
- 가격 검사기 **1147 통과 · 실패 0** — 네 언어 × 400 · 1280 × 여덟 상태 · 늦게 나타남 8 · 배너 높이 24(네 언어 × 여섯 폭)
  ⚠ 배포 전 한 번은 **빈 출력**이 나왔다(까닭을 남기지 못했다) — 다시 돌려 1147/0 · 종료 0
- 사이트 검사기 **310** · 머리띠 **88** — 실패 0 · 메뉴 폭 **SITE-57 전과 같음**(en 761 9.8) · 판매 단추 그대로
- 주석 여닫는 수 ko **57/57** · en **75/75** · ja **75/75** · zh **76/76**

## 배포 전 지문

라이브(배포 전) `/` `201d07dc` · `/en/` `f5453b1f` · `/ja/` `fc451697` · `/zh/` `0c909e00` · privacy 셋 `00bab904` · `1421d16f` · `b8e67629`
기대(로컬) `/` `2e370f5d` · `/en/` `4e65107e` · `/ja/` `c827f933` · `/zh/` `1a85f31b`

## 미검증

- 고친 검사기는 **스크래치패드**에 있다(`…/scratchpad/tools/check_hint2.py`) — 이 저장소에는 검사기를 두지 않는다(SITE-55 부터 같은 자리)
- 가짜 응답 처리기의 `time.sleep`(다른 응답까지 막는 것) 자체는 **안 고쳤다** — 전제 검사가 그 영향을 가린다. 고치려면 처리기를 비동기로 바꿔야 한다
